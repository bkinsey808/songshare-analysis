[pytest]
markers =
    essentia: mark tests that require Essentia and audio toolchain (ffmpeg, soundfile)

# Only collect tests from the top-level `tests/` directory. This avoids module
# name collisions with files living under `src/` that happen to share the same
# basename as test modules.
testpaths = tests
